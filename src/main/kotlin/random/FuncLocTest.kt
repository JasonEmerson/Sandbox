package random

class Solution(nums: IntArray) {
    var numArray = nums

    /** Resets the array to its original configuration and return it. */
    fun reset(): IntArray {
        return numArray
    }

    /** Returns a random shuffling of the array. */
//    fun shuffle(): IntArray {
//        var shuffledNums = numArray.shuffle()
//        return shuffledNums
//    }

}


